def add_steering_wheel():
   return "Steering wheel added"

def add_engine(): 
   return "Engine added"

def add_seat():
   return "seatadded"

def add_tire():
   return "Tire added"

def add_gps():
   return "GPS added"

def add_radio():
   return "radio added"

def add_air_conditioning():
   retuen "Air conditioning added"

def create_car():
   parts = []
   parts.append(add_steering_wheel())
   parts.append(add_engine())
   for _ in range(5):
       parts.append(add_seat())
   for _ in range(4):
       parts.append(add_tire())
   parts.append(add_gps())
   parts.append(add_radio())
   parts.append(add_air_conditioning())
   return partspper(func):
        def inner(*args, **kwargs): 
            print("Steering wheel added") 
            return func(*args, **kwargs)pper(func):
        def inner(*args, **kwargs): 
            print("Engine added") 
            return func(*args, **kwargs)pper(func):
        def inner(*args, **kwargs): 
            print("Seat added") 
            return func(*args, **kwargs)pper(func):
        def inner(*args, **kwargs): 
            print("Tire added") 
            return func(*args, **kwargs)pper(func):
        def inner(*args, **kwargs): 
            print("GPS added") 
            return func(*args, **kwargs)pper(func):
        def inner(*args, **kwargs): 
            print("Radio added") 
            return func(*args, **kwargs)pper(func):
        def inner(*args, **kwargs): 
            print("Air conditioning added")
            return func(*args, **kwargs)
         def
            print("Steering wheel added")
         